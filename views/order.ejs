<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Замовлення - Аквасвіт</title>
  <link rel="stylesheet" href="/styles.css">
  <!-- Bootstrap 5 for responsive design and components -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <header>
    <div class="header-container">
      <h1 class="brand">Аквасвіт</h1>
      <div class="contact">
        <a href="tel:0445001212">(044) 500-12-12</a>
        <span>Графік роботи: Пн-Пт 9:00-18:00</span>
      </div>
      <a href="/order" class="btn order-btn">Замовити воду</a>
    </div>
  </header>

  <main>
    <section class="hero">
      <h2>Зробіть ваше замовлення швидко і просто! <br><span class="highlight">Питна вода за розумною ціною</span></h2>
    </section>

    <div class="container my-5">
      <h2>Форма для замовлення води</h2>
      <form action="/create_order" method="POST" class="mt-4">
        <div class="mb-3">
            <label for="customerName" class="form-label">Ім'я замовника</label>
            <input type="text" class="form-control" id="customerName" name="customerName" required>
          </div>
          
          <div class="mb-3">
            <label for="customerSurname" class="form-label">Прізвище замовника</label>
            <input type="text" class="form-control" id="customerSurname" name="customerSurname" required>
          </div>
          
          <div class="mb-3">
            <label for="customerPhone" class="form-label">Номер телефону</label>
            <input type="tel" class="form-control" id="customerPhone" name="customerPhone" required>
          </div>
          
          <div class="mb-3">
            <label for="customerAddress" class="form-label">Адреса</label>
            <input type="text" class="form-control" id="customerAddress" name="customerAddress">
          </div>
          
          <div class="mb-3">
            <label for="customerEmail" class="form-label">Електронна пошта</label>
            <input type="email" class="form-control" id="customerEmail" name="customerEmail">
          </div>

        <div class="mb-3">
          <label for="productSelect" class="form-label">Оберіть продукт</label>
          <select class="form-select" id="productSelect" name="productId" required>
            <% products.forEach(product => { %>
              <option value="<%= product.id %>"><%= product.name %> - <%= product.discountPrice %> грн</option>
            <% }); %>
          </select>
        </div>

        <div class="mb-3">
          <label for="paymentType" class="form-label">Тип оплати</label>
          <select class="form-select" id="paymentType" name="paymentType" required>
            <option value="1">Готівка</option>
            <option value="2">Картка</option>
            <option value="3">PayPal</option>
          </select>
        </div>

        <button type="submit" class="btn btn-primary">Додати в кошик</button>
      </form>

      <!-- Відображення кошика -->
      <h3 class="my-4">Ваш кошик:</h3>
      <% if (busketItems && busketItems.length > 0) { %>
        <ul class="list-group">
          <% busketItems.forEach(item => { %>
            <li class="list-group-item d-flex justify-content-between align-items-center">
              <span><%= item.product_name %> - <%= item.product_price %> грн</span>
              <form action="/remove_from_busket/<%= item.id %>" method="POST">
                <button type="submit" class="btn btn-danger btn-sm">Видалити</button>
              </form>
            </li>
          <% }); %>
        </ul>
      <% } else { %>
        <p>Ваш кошик порожній.</p>
      <% } %>

    </div>
  </main>

  <footer>
    <div class="contact-form">
      <h3>Швидкий зв'язок</h3>
      <form action="/request" method="POST">
        <input type="tel" name="phone" placeholder="Введіть Ваш телефон" required>
        <button type="submit" class="btn submit-btn">Залишити заявку</button>
      </form>
    </div>
  </footer>

</body>
</html>
