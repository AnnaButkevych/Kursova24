<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <div class="header-container">
      <h1 class="brand">Аквасвіт</h1>
      <div class="contact">
        <a href="tel:0445001212">(044) 500-12-12</a>
        <span>Графік роботи</span>
      </div>
      <a href="/order" class="btn order-btn">Замовити воду</a>
    </div>
  </header>

  <main>
    <section class="hero">
      <h2>КОРИСНА ВОДА <br><span class="highlight">розумна ціна</span></h2>
    </section>

    <section class="products">
      <% products.forEach(product => { %>
        <div class="product">
          <img src="<%= product.image %>" alt="<%= product.name %>">
          <h3><%= product.name %></h3>
          <p><%= product.description %></p>
          <div class="price">
            <span><%= product.regularPrice %> грн</span>
            <span class="discount"><%= product.discountPrice %> грн</span>
          </div>
        </div>
      <% }); %>
    </section>
  </main>

  <footer>
    <div class="contact-form">
      <h3>Швидкий зв'язок</h3>
      <form action="/request" method="POST">
        <input type="tel" name="phone" placeholder="Введіть Ваш телефон" required>
        <button type="submit" class="btn submit-btn">Залишити заявку</button>
      </form>
    </div>
  </footer>
</body>
</html>